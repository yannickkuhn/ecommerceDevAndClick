{% extends 'baseAdmin.html.twig' %}

{% block body -%}
    <h1>Création d'un nouveau produit</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('adminProducts') }}">
            Revenir à la liste
        </a>
    </li>
</ul>
{% endblock %}
