{% extends 'baseAdmin.html.twig' %}

{% block title %}Tva{% endblock %}

{% block body %}
    <h1>Tva</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tva.id }}</td>
            </tr>
            <tr>
                <th>Multiplicate</th>
                <td>{{ tva.multiplicate }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ tva.nom }}</td>
            </tr>
            <tr>
                <th>Valeur</th>
                <td>{{ tva.valeur }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('adminTva_index') }}">Retour à la liste</a>

    <a href="{{ path('adminTva_edit', {'id': tva.id}) }}">Editer</a>

    {{ include('Administration/Tva/_delete_form.html.twig') }}
{% endblock %}
